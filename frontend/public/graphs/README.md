# Graphs for graph_choice tasks

Place SVGs referenced by `image_url` in tasks_v2.json here, e.g.
`graphs/dt_horizontal.svg` → `frontend/public/graphs/dt_horizontal.svg`.

If a file is missing, the player falls back to `image_description` text.
